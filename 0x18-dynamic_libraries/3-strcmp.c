#include "main.h"
#include <string.h>

/**
 * _strcmp -> Afuntion that compares two strings
 * @s1: First parameter
 * @s2: Second parameter
 * Return: int value
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
