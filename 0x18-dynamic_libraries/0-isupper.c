#include "main.h"

/**
 * _isupper - Check if a function is uppercase
 * @c: The parameter to be checked
 * Return: 1 for upper letter or 0 for any else
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
