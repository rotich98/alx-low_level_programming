#include "main.h"
/**
 * _islower -> checks whether the character is lowercase
 * @c:argument characters.
 * Return: Return 1 or 0 based on the condition.
 */
int _islower(int c)
{
return (c >= 'a'; && c <= 'z');
}

