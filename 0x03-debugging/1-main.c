#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */
int main(void)
{
	int i;
	/*
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("infinit loop avoided! \\o/\n");
	return (0);
}
